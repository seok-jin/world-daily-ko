# 푸시 자격증명 (운용 환경에 export 또는 systemd EnvironmentFile로 주입)

# AI 백엔드 선택
BBC_AI_BACKEND=claude          # 로컬 Windows 테스트
# BBC_AI_BACKEND=gemini        # EC2 운용

# Telegram (간편)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Streamlit 뷰어 URL (Telegram 메시지에 포함됨)
REPORT_BASE_URL=http://3.37.118.119:8504

# 이메일 (Gmail 예시)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=                     # Gmail은 앱 비밀번호 (16자)
EMAIL_TO=                      # 받을 이메일 주소
